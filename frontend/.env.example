# Base URL for the backend API.
# Leave empty for local dev (Vite proxy forwards /api to localhost:8000).
# Set to a full URL for deployed environments, e.g. https://api.example.com
VITE_API_BASE_URL=
